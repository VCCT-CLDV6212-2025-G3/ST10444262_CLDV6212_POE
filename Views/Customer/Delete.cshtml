@model ST10444262_CLDV6212_POE.Models.Customer

@{
    ViewData["Title"] = "Delete Customer";
}

<div class="container mt-4">
    <h1 class="mb-4 fw-bold text-danger"><i class="bi bi-exclamation-triangle-fill me-2"></i> Delete Customer</h1>
    <h4 class="mb-3">Are you sure you want to delete this customer?</h4>

    <div class="card shadow-sm border-0 p-4">
        <dl class="row">
            <dt class="col-sm-4 fw-semibold">
                @Html.DisplayNameFor(model => model.CustomerName)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.CustomerName)
            </dd>
            <dt class="col-sm-4 fw-semibold">
                @Html.DisplayNameFor(model => model.CustomerEmail)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.CustomerEmail)
            </dd>
            <dt class="col-sm-4 fw-semibold">
                @Html.DisplayNameFor(model => model.CustomerId)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.CustomerId)
            </dd>
        </dl>

        <form asp-action="Delete" asp-route-id="@Model.RowKey" method="post">
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-danger rounded-pill px-4">
                    <i class="bi bi-trash-fill me-2"></i> Confirm Delete
                </button>
                <a asp-action="GetAll" class="btn btn-outline-secondary rounded-pill px-4">
                    <i class="bi bi-arrow-left-circle-fill me-2"></i> Back to List
                </a>
            </div>
        </form>
    </div>
</div>