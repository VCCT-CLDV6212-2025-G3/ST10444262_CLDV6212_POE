@model ST10444262_CLDV6212_POE.Models.Customer

@{
    ViewData["Title"] = "Delete Customer";
}

<div class="container mt-4">
    <h1 class="mb-4 text-danger fw-bold"><i class="bi bi-trash me-2"></i> Delete Customer</h1>

    <div class="alert alert-warning rounded-pill px-4">
        <i class="bi bi-exclamation-triangle-fill me-2"></i> Are you sure you want to delete this customer?
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Name</dt>
                <dd class="col-sm-9">@Model.CustomerName</dd>

                <dt class="col-sm-3">Email</dt>
                <dd class="col-sm-9">@Model.CustomerEmail</dd>

                <dt class="col-sm-3">Customer ID</dt>
                <dd class="col-sm-9">@Model.CustomerId</dd>
            </dl>

            <form asp-action="DeleteCustomer" method="post">
                <input type="hidden" asp-for="RowKey" />
                <button type="submit" class="btn btn-outline-danger rounded-pill px-4">
                    <i class="bi bi-trash me-2"></i> Delete
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">
                    <i class="bi bi-arrow-left me-2"></i> Cancel
                </a>
            </form>
        </div>
    </div>
</div>
